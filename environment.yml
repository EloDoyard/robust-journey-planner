name: "base"
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - matplotlib
  - ipywidgets
  - conda-forge::voila=0.2.7
  - networkx
  - plotly=4.14.3
  - hdfs3
  - pip
  - pip:
      - scikit-learn
      - geopandas
      - ipydatetime

# - add packages here
# - one per line
prefix: "/opt/conda"

